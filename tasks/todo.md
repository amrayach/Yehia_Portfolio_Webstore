# Task: Connect Local Project To GitHub And Create Initial Commits

## Plan
- [x] Initialize git in this project and set default branch to `main`.
- [x] Add baseline repo files (`README.md` and `.gitignore`) for a clean first commit.
- [ ] Configure `origin` as `git@github.com:amrayach/Yehia_Portfolio_Webstore.git`.
- [ ] Stage all intended files and create the initial commit.
- [ ] Push `main` to `origin` and verify remote tracking.

## Progress Notes
- Plan created.
- Git initialized with `main`.
- Added baseline `README.md` and `.gitignore`.

## Verification
- [ ] Confirm branch and status with `git status`.
- [ ] Confirm commit exists with `git log --oneline -n 1`.
- [ ] Confirm remote mapping with `git remote -v`.
- [ ] Confirm upstream branch with `git branch -vv`.

## Review
- Pending.
