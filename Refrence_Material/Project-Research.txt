

Client: Yehia Moldan (see 'who is yehia moldan provide me with information.md' for details)
Project: Portfolio / Store Website
- The client has provided their vision and website references in 'Client-Brief-vision.txt'.
- Reference images and the logo (included) should be used as inspiration.
- The main design theme is minimalistic, with a dominant black and white color palette.
Begin with a concise checklist (3-7 bullets) outlining your approach before brainstorming solutions.
Please brainstorm ideas for this project:
- Suggest a suitable technical stack (frameworks, languages, hosting, etc.)
- Recommend libraries/tools for front-end and back-end implementations
- Advise on UI/UX strategies that align with a minimal and monochrome aesthetic
- Share additional tips for successfully initiating development
List any clarifying questions that would help better understand the client's vision or address ambiguities.
Set reasoning_effort = High to ensure depth and actionable recommendations.

### Approach checklist (high-level)

* Extract **site map + interaction rules** (where scrolling is allowed vs. forbidden) and lock the navigation model.
* Define a **content model** (collections, works, editorial “issues”, PDFs/articles, contact) and decide CMS vs. repo-based content.
* Build a **minimal B/W design system** (type scale, spacing, grid, hairline rules, motion principles).
* Prototype the **home 3D centerpiece** (performance budgets + mobile fallback).
* Implement **Shop as “registry / waitlist”** (everything “sold out” by design) + decide the **payment flow** (crypto/stablecoin deposit vs inquiry-only).
* Ship an MVP with analytics/SEO, then iterate on editorial storytelling and commerce mechanics.

---

## 1) Suitable technical stack (frameworks, languages, hosting)

### Recommended (best balance: 3D + portfolio + editorial + store)

**Frontend**

* **Next.js (App Router) + TypeScript**
* **Tailwind CSS** for strict monochrome theming and spacing discipline
* **React Three Fiber (three.js)** for the rotating 3D object on the landing page (centerpiece requirement). 

**Backend (lightweight)**

* Next.js **Server Actions / Route Handlers** (no separate server needed initially)
* **Supabase (Postgres + Auth + Storage)** *or* **Cloudflare D1/R2** depending on hosting preference

  * Use DB for: registry reservations, waitlist entries, edition numbers, inquiry tracking.

**Hosting**

* **Vercel** (fastest Next.js deployment; good for motion/3D demos)
* Alternative: **Cloudflare Pages** (excellent global CDN; pair with R2 for media)

**Content**

* If Yehia wants to update content often without dev help: **Sanity** (headless CMS)
* If updates are rare and curated: **MDX in-repo** (versioned editorial, articles, “text” PDFs)

This aligns cleanly with the requested structure: Portfolio/editorial focus + “Shop” collections that are intentionally out-of-stock. 

---

## 2) Libraries / tools to recommend

### Front-end UI + motion (monochrome-friendly)

* **shadcn/ui + Radix UI** (clean primitives, minimal styling, accessible by default)
* **Framer Motion** (page transitions, subtle reveals, hover states, “issue-like” editorial navigation)
* **next/image** + AVIF/WEBP pipeline for photography-heavy pages (jewelry/editorial)

### 3D / landing centerpiece

* **@react-three/fiber** + **@react-three/drei**
* **glTF (GLB) pipeline** from Blender/C4D (optimize meshes + Draco compression)
* **Performance fallback**: static hero image/video loop on low-power devices (important for mobile)

### Editorial / “no scrolling” navigation

* **Keen Slider** or **Embla Carousel** (keyboard + arrows; behaves like “pages” without scroll)
* Optional: **GSAP** (only if you need very art-directed motion)

### Shop / registry mechanics

Because the shop is “sold out” by design, make the CTA about **registry placement**, **deposit**, or **DM to finalize**—matching the Love collection doc (“Reply to our DM… confirm your place in the registry”). 

Suggested modules:

* Waitlist + registry form (DB)
* “Edition counter” (reserved / remaining)
* Admin-lite panel (even a protected route) to mark items reserved/fulfilled

### Payments (Stripe constraint + crypto/stablecoin options)

Your brief explicitly notes Stripe issues in Tunisia. 
Stripe’s official supported-country list does **not** include Tunisia. ([Stripe][1])

Two credible paths:

1. **BTCPay Server (self-hosted)**

* Self-hosted, open-source crypto payment processor; funds go directly to your wallet. ([btcpayserver.org][2])
* Has documented e-commerce integration patterns. ([docs.btcpayserver.org][3])
  Best when you want maximal control and minimal third-party dependency.

2. **Triple-A (stablecoin checkout)**

* Positions itself as a digital-currency/stablecoin checkout with merchant settlement options. ([triple-a.io][4])
* Listed as a Visa partner / accelerator cohort on Visa’s partner directory. ([partner.visa.com][5])
  Best when you want a “clean checkout UI” with higher user trust and less operational overhead (as your brief suggests). 

**Pragmatic recommendation for MVP:**
Because all items are “out of stock” anyway, start with **registry + deposit invoice** (crypto/stablecoin) rather than a full cart/checkout. It matches the Love collection acquisition flow (50% deposit → craftsmanship → finalization → delivery). ---

## 3) UI/UX strategies for minimal + monochrome

### Information architecture (from the brief)

* **Yehia Moldan (Home/Portfolio landing)** — no scrolling; central 3D rotation. 
* **Editorial** — no scrolling; treat as “issues/pages” navigated by arrows/keyboar
* **Shop / Text / Work** — scrolling allowed. 

### Visual system principles (B/W, minimal, high-end)

* **Strict palette:** #000 / #fff + 1 neutral gray; le “color” (gold tones) while UI remains monochrome.
* **Hairline rules + generous whitesparic line language in the provided logos.
* **Typolarge editorial headlines, restrained body copy, precise tracking/leading.
* **Micro-interactions, not heavy effects:** hover reveals, subtle fades, scroll-based parallax only where allowed.

### Interaction patterns that fit “no scrolling”

* **Full-viewport “frames”** with:

  * Left/right arrows (or next/prev) = navigate editorial works
  * Keyboard support (← →, ESC)
  * URL state per frame (`/editorial/self-mashallah/01`) for shareability
* Home: 3D object center + minimal nav at edges (top-left logo, top-right menu, bottom-left “enter shop”, bottom-right “contact”)

### Shop UX (sold-out as a feature)

* Product cards always show **SOLD OUT** but with elevated CTA:

  * “Join Registry”
  * “Request Edition Number”
  * “Reserve with 50% deposit”
* Add a **registry narrative layer**:

  * Limited editions, numbered pieces, certificate of authenticity (already in Love doc). 

---

## 4) Tips to successfully initiate development (MVP-first)

1. **Lock the navigation + scroll rules first**
   This dictates routing, transitions, and layout architecture more than visuals do.

2. **Decide content ownership**

* If Yehia will update often → CMS (Sanity)
* If curated + asset pipeline

3. **Prototype the 3D centerpiece early**

* Establish polygon budget, load strategy, and mobile fallback.
* Don’t let 3D become a late-stage risk.

4. **Treat “Shop” as “Acquisition System”, not e-commerce**

* Implement registry + deposit + manual finalization (DM), consistent with the Love acquisition steps. 

5. **Performance + SEO guardrails**

* Lazy-load 3D, prefetch routes, compress images, add structured metadata for collections/works.

---

## Clarifying questions (to remove ambiguities)

1. **Languages:** English only, Arabic only, or bilingual toggle (and does RTL layout apply site-wide?
2. **3D asset:** What exactly rotates on the landing page—logo mark, “حب” sculpture, or a new 3D form? Do you already have a GLB?
3. **Editorial “no scrolling”:** Is *horizontal paging* acceptable (via arrows/keys), or must it be strictly a single static viewport with popups?
4. **Shop intent:** If everything is “out of stock,” do you still want per-product pages and a registry/deposit flow, or purely “inquiry/DM”?
5. **Payments:** Are you aiming for (a) crypto-only deposits, (b) stablecoin checkout, or (c) both + manual bank transfer fallback?
6. **Inventory logic:** Should “limited to 50” (and “first 10 special rate”) be enforced programmatically for the Love set? 
7. **Work section assets:** For logos—PDF uploads, Behance embeds, or outbound link only? 

If you want, I can turn this into a concrete MVP spec (routes, components, content schemas, and a 2–3 sprint implementation plan) consistent with the brief. 



[1]: https://stripe.com/en-de/global "Stripe global availability"
[2]: https://btcpayserver.org/?utm_source=chatgpt.com "BTCPay Server"
[3]: https://docs.btcpayserver.org/Development/ecommerce-integration-guide/?utm_source=chatgpt.com "eCommerce Integration Guide"
[4]: https://www.triple-a.io/digital-currency-payments?utm_source=chatgpt.com "Crypto Payment Gateway | Accept Stablecoin & Digital Currency"
[5]: https://partner.visa.com/site/partner-directory/triple-a.html?utm_source=chatgpt.com "TripleA"


1. English only for now.
2. Logo for now, I don't have a glb file still
3. I don't know
4. Only inquiry/DM for now 
5. The easy option (probably stablecoin checkout and manual back transfer as fallback but the pyment part could come later. 
6. I don't know.
7. I don't know 

Additional information I want to keep the implementation simple and the deployment as well therefore I want to go with a more static approach in which the project uses a github repo which is deployed on a free cloudflare deplyoment plan with a already bought domain. 

I have additional information from the codex cli that ran and planned locally. 

- Extract concrete constraints from the brief, AGENTS notes, and visual assets.
  - Define a primary stack and one fallback stack based on launch speed, maintainability, and visual performance.
  - Map front-end and back-end tools to each website section (portfolio, editorial, shop, text, work, contact).
  - Propose UI/UX rules for minimal monochrome design with the required no-scroll pages.
  - Flag delivery risks early (crypto payments, content operations, asset pipeline) and list clarifying questions.

  Skill usage: frontend-design-claude (distinctive high-end visual direction), frontend-design-codex (clarity/spacing/CTA discipline), and vercel-react-best-practices (React/Next performance
  defaults).

  ### Recommended technical stack

  1. Primary stack (recommended)

  - Next.js 16 + TypeScript + App Router
  - React 19.2.x (latest patched line)
  - Styling with Tailwind CSS + strict CSS variables/tokens for brand consistency
  - 3D hero with react-three-fiber + @react-three/drei + optimized glTF
  - Content via Sanity (or Payload if you prefer fully self-hosted)
  - Data/forms via Supabase (Postgres + storage) and Next route handlers
  - Hosting: Vercel for fastest Next.js deployment/preview workflow

  2. Alternative stack (if edge-first / Cloudflare preference)

  - Same Next.js app deployed on Cloudflare Workers using OpenNext adapter
  - Cloudflare D1 for lightweight SQL data, R2 for assets
  - Good fit if you want Cloudflare-native infra and global edge routing by default

  ### Front-end libraries/tools

  - react-three-fiber, @react-three/drei: rotating 3D landing centerpiece
  - framer-motion (or Motion): restrained transitions and section reveals
  - Lenis optional for smooth scroll only on allowed pages (Shop, Text, Work)
  - next/image: optimized editorial/product images
  - zod + react-hook-form: robust contact/inquiry forms
  - next-intl if bilingual Arabic/English is needed
  - Plausible or PostHog: privacy-conscious analytics

  ### Back-end / commerce tooling

  - Next.js Route Handlers for lightweight API endpoints
  - Sanity/Payload for collections, editorials, and metadata management
  - Supabase for inquiries, waitlists, and stock state (out_of_stock flags)
  - Crypto payments:
  - Practical phase 1: keep everything out-of-stock and use “Join waitlist / Request purchase”
  - Phase 2: integrate Triple-A hosted/embedded checkout for stablecoin flow
  - Phase 3 (optional): BTCPay Server if you want non-custodial/self-hosted control and can handle ops overhead

  ### UI/UX strategy for minimal monochrome aesthetic

  - Use a strict black/white/gray token system; reserve accent color only for interaction state
  - Make typography the hero: large editorial headlines, short copy blocks, generous negative space
  - Build no-scroll pages as viewport scenes:
  - Yehia Moldan: one-screen stage with keyboard/touch scene switching
  - Editorial: full-screen slide/story frames (no vertical scroll)
  - Scroll-enabled pages:
  - Shop: vertical collection grid with strong “Out of Stock” treatment
  - Text: long-form reading mode with perfect measure/line-height
  - Work: modular gallery by category (tattoos/logos/jewelry)
  - Motion principles:
  - Slow, intentional 3D rotation; subtle fade/translate for content
  - Support prefers-reduced-motion for accessibility
  - Composition from your references:
  - High-contrast editorial layout
  - Strong geometric iconography inspired by supplied logos/symbols
  - Clean product presentation with shadow-light photography handling

  ### Additional tips to start development successfully

  - Lock IA first: route map + what is/no-scroll before coding components
  - Create design tokens/motion specs in week 1 (colors, spacing, type scale, transitions)
  - Define content model early (collections, works, editorials, PDF/text assets, contact entries)
  - Set performance budgets now:
  - 3D asset budget (target compressed glTF, lazy load)
  - Image sizes per breakpoint
  - LCP target under 2.5s on mobile
  - Plan legal/compliance early for crypto checkout and regional policies
  - Add preview workflow (CMS draft previews + Vercel/Cloudflare preview deployments)

  ### Clarifying questions

  1. Is launch priority brand portfolio presence or shop conversion? i am not sure but I think porfolio
  2. Should all shop products remain permanently out-of-stock, or do you want timed drops later? permanently out-of-stock
  3. For crypto checkout, do you want custodial convenience (Triple-A) or non-custodial control (BTCPay)? Triple-A I think
  4. Do you need bilingual content (English + Arabic) at launch? English only for now 
  5. Should “Logos” in Work open external Behance links, or be hosted natively as PDFs/pages? I don't know
  6. For Yehia Moldan and Editorial no-scroll pages, do you prefer scene changes by click/swipe/arrows? I don't know 
  7. Do you want CMS-editable content for all sections, or only for shop/editorial/text? I don't know
  8. Should “Text” keep the PDF as-is, or be converted into SEO-friendly article pages too? I don't know 
  9. What is the launch timeline and who will maintain content after handoff? as fast as possible since it's a non-paid gig and there will be no maintenance therefore I will use a static page approach in which the repo changes the deployment changes 
  10. Do you already have domain/hosting preference (Vercel vs Cloudflare)? Cloudflare but it will come later

  ### Inputs reviewed

  - AGENTS.md
  - Client-Brief-vision.txt
  - who is yehia moldan provide me with information.md
  - Provided logo and image assets in /home/amay/Work/Yehia_webstore/Refrence_Material/logo and /home/amay/Work/Yehia_webstore/Refrence_Material/images

  ### External sources checked (current as of March 2, 2026)

  - Next.js docs: https://nextjs.org/docs
  - React blog/security updates: https://react.dev/blog
  - Vercel Next.js docs: https://vercel.com/docs/frameworks/nextjs
  - Vercel limits/pricing: https://vercel.com/docs/limits , https://vercel.com/pricing
  - Cloudflare Next.js on Workers: https://developers.cloudflare.com/workers/framework-guides/web-apps/nextjs/
  - Cloudflare D1: https://developers.cloudflare.com/d1/
  - BTCPay docs: https://docs.btcpayserver.org/Guide/ , https://docs.btcpayserver.org/Deployment/ , https://docs.btcpayserver.org/Development/ecommerce-integration-guide/
  - Triple-A integration/docs: https://support.triple-a.io/knowledge/integration-options-with-triplea , https://www.triple-a.io/digital-currency-payments


If any questions / clarifications are still open re ask them in simpler way 


According to a document from **2026-03-02** (your uploaded project brief), the site must be **black/white**, have a **3D rotating centerpiece on the landing page**, enforce **no scrolling on “Yehia Moldan” + Editorial**, and allow scrolling on **Shop/Text/Work**; the Shop collections should appear **out-of-stock by design**, and payments are complicated by Stripe not working in Tunisia (Triple-A / BTCPay were considered).

## Concrete constraints (locked-in now)

* **Language:** English only (for launch).
* **Hosting/deploy:** GitHub repo → **Cloudflare Pages (free)** + existing domain (keep it static + simple).
* **Home:** rotating centerpiece **using the logo for now** (no GLB yet).
* **Editorial:** no-scroll experience (but you’re undecided on *how* navigation should feel).
* **Shop:** **inquiry/DM only** (no real checkout at MVP).
* **Maintenance:** “as fast as possible”, **no ongoing content ops** → content should live in the repo and redeploy on change.

---

## Stack recommendation for a “static-first / Cloudflare Pages” build

### Primary stack (best for speed + minimal ops)

**Astro (static output) + TypeScript + Tailwind**
Why: Astro is designed for content-heavy sites with **very low JS by default** and deploys cleanly to **Cloudflare Pages**.

* **Astro**: static pages, great for editorial/portfolio content
* **Tailwind**: tight monochrome token system + consistent spacing
* **MDX (optional)**: for “Text” articles if you want them as readable pages
* **Zero backend** for MVP (DM links + mailto)

### Fallback stack (if you insist on Next later)

**Static Next.js on Pages** (still doable, but adds adapter complexity). Cloudflare supports Next.js on Pages; dynamic Next requires Workers/OpenNext/next-on-pages.

**My call:** go **Astro-first** for this specific “no maintenance / static / Cloudflare” constraint.

---

## Section-by-section: tools + implementation mapping

### 1) **Home (Yehia Moldan)** — *no scroll, 1-screen stage*

Goal from brief: “first landing page is a 3d design rotating in the middle”.

Because there’s no GLB yet:

* Do a **“fake-3D” logo rotation**:

  * Convert logo to **SVG** (vector).
  * Animate with **CSS 3D transforms** + subtle shading / blur + slow orbit line (your logo2 ellipse is a perfect motif).
* Optional upgrade (still without GLB): **three.js extrude from SVG path** (more work; keep as phase 2).

Minimal libs:

* Vanilla CSS animation or **motion-one** (tiny) instead of heavy animation stacks.

### 2) **Editorial (Self-Mashallah / Sindbad)** — *no scroll “pages”*

Brief requires no scrolling for editorial.

Implementation pattern (static-friendly):

* Use a **full-viewport “frame” system**:

  * `height: 100vh; overflow: hidden;`
  * navigate via **←/→ keys**, click zones, and/or swipe
* Use a lightweight carousel:

  * **Embla** (small, clean) or **Keen Slider**
* Each “frame” gets a URL:

  * `/editorial/self-mashallah/01`
  * shareable + SEO safe

### 3) **Shop** — *scroll allowed, inquiry-only, “out-of-stock” as a feature*

Brief lists collections and explicitly says **all must be out-of-stock** for demand.

MVP behavior:

* Grid of products/collections with **SOLD OUT** overlay.
* Primary CTA: **“DM to inquire”** (Instagram deep link) + secondary “Email”.
* Optional: one “Process” page summarizing how reservations work (inspired by the Love registry flow).

### 4) **Text** — *scroll allowed*

Brief says: “Text (a pdf book) - two articles”.

Static-simple options:

* **Option A (fastest):** upload PDFs + embed viewer + download link
* **Option B (better UX/SEO):** convert the two articles to **MDX pages** and offer the PDF as downloadable

### 5) **Work** — *scroll allowed*

Brief includes: tattoos, logos (PDF or Behance link), jewellery.

Static-friendly layout:

* `/work` → three category cards
* `/work/tattoos` (gallery)
* `/work/jewellery` (gallery)
* `/work/logos`:

  * either **PDF grid** in-site **or** link out to Behance

### 6) **Contact**

Keep it simple:

* Instagram
* Email (mailto)
* Optional WhatsApp

If you *really* want a form later: Cloudflare Pages **Functions** can handle form submissions, but that adds “some backend”.

---

## UI/UX rules for “minimal monochrome” (aligned with your assets)

Your logos and reference images suggest: **hard geometry + hairlines + big whitespace + editorial typography**.

### Design system rules

* Palette: **#000 / #fff + one neutral gray** (for borders/secondary text only)
* Borders: **1px hairlines** everywhere (cards, dividers, nav)
* Layout: strict **grid + large margins**
* Typography: make type the hero (high-end editorial feel)

  * Serif for headlines + neutral sans for UI (or one variable font with strong contrast)
* Motion: slow, intentional, almost “museum label” pace

### Motifs from your visuals (use consistently)

* **Diagonal split lines** (like your “DREAM” image)
* **Orbit ellipse** (logo2)
* **Chevron / angular strokes** (logo1)

---

## Delivery risks to flag early (and how to neutralize them)

1. **No GLB 3D asset yet** → don’t block launch.
   Ship a logo “pseudo-3D” rotation now; swap to real 3D later.

2. **No maintenance** → avoid CMS + avoid complex build chains.
   Keep content in repo with a simple structure (MD/MDX + images).

3. **Inquiry-only shop** → you still need strong conversion UX.
   Make “DM to inquire” frictionless (one tap), and show a clear “process” page (reservation, timelines, etc.).

---

## A simple repo structure that stays maintainable

* `src/pages/` → routes
* `src/content/`

  * `editorial/` (one folder per issue, frames as markdown/MDX)
  * `shop/` (collections as JSON)
  * `text/` (articles as MDX or PDFs)
  * `work/` (gallery metadata)
* `public/assets/` → images + PDFs
* `src/styles/` → tokens + globals

Cloudflare Pages deploy is straightforward for static frameworks like Astro (Git integration).

---

## Re-asking the open questions in a simpler way

Answering these (even with “A/B”) will remove the remaining ambiguity:

1. **Editorial navigation:** should it feel like **(A)** a slide deck (left/right), or **(B)** a “chapters” list where you open one piece at a time?
2. **Editorial content shape:** do you want **(A)** one long editorial series split into frames, or **(B)** multiple separate editorials each with 5–15 frames?
3. **Logos section:** should logos open as **(A)** PDFs on your site, or **(B)** external Behance links?
4. **Text section:** do you prefer **(A)** PDFs only, or **(B)** readable article pages (with optional PDF download)?
5. **Primary DM channel:** Instagram DM only, or should we also include WhatsApp/email as equal CTAs?
6. **Homepage nav:** minimal top nav always visible, or a hidden menu (hamburger) to keep the stage cleaner?

If you answer just those 6, I can propose a concrete **site map + wireframe-level layout rules** (per page) that’s ready to implement in a static Astro repo on Cloudflare Pages.


1. (B) Chapters list / page navigation (with occasional prev/next between items)
2. (B) Multiple separate pieces/pages, not one long continuous editorial
3. A 
4. B for the two articles (MDX pages), plus the PDF download.
5. Primary IG DM + fallback Email
6. A But make it hairline, small, unobtrusive, so the centerpiece still dominates.

According to the project brief you shared (reviewed **2026-03-02**), the site must be **black/white**, the landing page must feature a **rotating 3D centerpiece**, **no scrolling** is allowed on **Yehia Moldan + Editorial**, and scrolling is allowed on **Shop/Text/Work**; Shop collections should appear **out-of-stock by design** and payments can come later.

## Locked decisions (based on your answers)

* **English only** (for now)
* **Static-first + simple ops**: GitHub repo → **Cloudflare Pages (free)** + your domain
* **Home centerpiece**: rotate the **logo** (no GLB yet)
* **Editorial UX**: **chapters list / page navigation** (with occasional prev/next)
* **Editorial structure**: **multiple separate pieces/pages** (not one long continuous story)
* **Logos**: host **PDFs on-site**
* **Text**: the **two articles as MDX pages** + **PDF download**
* **Shop**: **Inquiry/IG DM only** (+ fallback email), no checkout MVP
* **Nav**: always visible but **hairline + unobtrusive**

---

## Primary stack (optimized for static Cloudflare Pages)

### Recommended

**Astro + TypeScript + Tailwind + MDX**
Reason: fastest path to a premium static site, minimal runtime JS, and clean Cloudflare Pages deployment (no server needed).

**Key add-ons**

* `@astrojs/tailwind`
* `@astrojs/mdx`
* `@astrojs/cloudflare` adapter (Cloudflare Pages build target)

**Minimal interactive JS**

* Use pure CSS for most motion (logo rotation, hover reveals)
* Use a tiny client script only where needed (e.g., Editorial chapter rail highlight, mobile menu)

### Fallback (only if you later need app-like dynamics)

Next.js on Cloudflare (Workers/adapter) — but it’s heavier than your “no maintenance” constraint.

---

## Site map + routing (matches the brief + your UX choices)

From the brief, the top-level sections are: **Yehia Moldan, Shop, Editorial, Text, Work, Contact**.

### Routes

* `/` **(Yehia Moldan / Home stage)** — **NO SCROLL**

  * rotating logo centerpiece
  * small “Featured Editorial” list (fits in viewport)
* `/editorial` — **NO SCROLL**

  * grid/list of editorial pieces (covers)
* `/editorial/[piece]/[chapter]` — **NO SCROLL**

  * left: chapter list (rail)
  * right: chapter content
  * occasional prev/next between chapters
* `/shop` — scroll

  * collections: Love / Love Classic / Shams / Prints (all “sold out”)
* `/shop/[collection]` — scroll

  * product grid (SOLD OUT overlay) + **“DM to inquire”**
* `/text` — scroll

  * 2 article cards (MDX) + PDF card
* `/text/[slug]` — scroll (MDX article)

  * “Download PDF” CTA
* `/work` — scroll

  * Tattoos / Logos / Jewellery categories
* `/work/tattoos` — scroll gallery
* `/work/jewellery` — scroll gallery
* `/work/logos` — scroll grid of **PDFs**
* `/work/logos/[slug]` — PDF viewer page (embed + download)
* `/contact` — scroll

  * Instagram DM primary + email fallback

---

## Content model (repo-only, zero CMS, easy to update)

Keep everything in the repo so “maintenance = commit + push”.

### Suggested structure

* `src/content/editorial/`

  * `self-mashallah/`

    * `meta.json` (title, year, cover, description)
    * `01.mdx`, `02.mdx`, …
  * `sindbad/`

    * same pattern
* `src/content/text/`

  * `article-1.mdx`
  * `article-2.mdx`
  * `book.pdf` in `public/`
* `src/content/shop/`

  * `collections.json`
  * `love.json`, `shams.json`, etc.
* `src/content/work/`

  * `tattoos.json`, `jewellery.json`
  * `logos/` (PDFs in `public/pdfs/logos/` + an index json for titles/thumbs)

This enables **Astro build-time route generation** for editorial chapters and logo PDFs.

---

## UI/UX rules (monochrome + hairline, aligned with your assets)

### Global design tokens

* Background: `#fff` (default), optional “inverted mode” sections (`#000`)
* Text: `#000` / `#fff`
* Borders/hairlines: `rgba(0,0,0,0.18)` (or 0.24) — consistent everywhere
* Spacing: strict scale (4/8/12/16/24/32/48…)

### Navigation (always visible, minimal)

* Top bar: 1px bottom border, small type, wide letterspacing
* Left aligned: wordmark (or “YM”)
* Right aligned: section links (or a tiny menu icon on mobile)
* Active state: underline hairline or a single pixel dot

### Home centerpiece (logo “pseudo-3D”)

Until you have a GLB:

* Convert logo to **SVG**
* Apply **CSS perspective + slow rotation** (10–18s loop)
* Add a second orbit line (from logo2) rotating subtly counter-direction
* Keep everything dead-center; nav must feel like “UI chrome”, not content

### Editorial (no scroll, chapter rail)

* Layout: `100vh`, `overflow: hidden`
* Left rail: chapters list (01, 02, 03…) + titles
* Content panel: one chapter visible at a time
* Motion: quick crossfade (150–250ms), nothing bouncy
* Occasional prev/next: tiny arrows at bottom corners (optional)

### Shop (scroll, inquiry-only)

* Product card states:

  * Default: image + name
  * Hover: SOLD OUT stamp + “DM to inquire”
* “DM” button should prefill a message:

  * `Hi Yehia — I’m interested in {collection} / {item}. Can you share availability and pricing?`

---

## “Simple to build” implementation plan (fast MVP)

### Phase 0 (1–2 hours)

* Create Astro repo, Tailwind, MDX, Cloudflare adapter
* Add base layout + typography + tokens

### Phase 1 (day 1)

* Implement routing skeleton + hairline nav + footer
* Implement `NoScrollLayout` wrapper for `/` and `/editorial/**`
* Implement Home stage with rotating SVG logo

### Phase 2 (day 2)

* Editorial content system:

  * build `getStaticPaths()` from `src/content/editorial/**`
  * chapter rail + chapter page layout + prev/next links
* Text:

  * 2 MDX pages + PDF download link
* Work:

  * category pages + logos PDF pages (embed + download)

### Phase 3 (day 3)

* Shop:

  * collections + product grids
  * sold out overlay + DM/email CTAs
* Polish: responsive typography, image optimization, basic SEO

---

## Remaining “simple” questions (only what still matters)

1. **How many editorial pieces** at launch (2? 5? 12?) and roughly **how many chapters per piece**?
2. For editorial chapters: are they mostly **images**, mostly **text**, or a mix?
3. Do you want the site background **always white**, or should some pages be **inverted (black)** for drama (still monochrome)?
4. What is the **exact Instagram handle** to hardcode for DM deep links (and the email address)?

If you answer those 4, I can give you a final “MVP spec” with exact page layouts (component list + file structure + content schemas) tailored to your static Cloudflare Pages setup.


Below is a **Codex CLI–friendly prompt pack** that keeps each session’s context small, repeatable, and merge-safe for a **static Astro + Tailwind + MDX** site deployed on **Cloudflare Pages**.

---

## 1) Recommended workflow (so sessions don’t blow the context window)

### Session protocol (use every time)

1. **Read only 2–4 repo docs** (short, canonical “truth”).
2. Produce a **plan + file list + risk notes** (no code yet).
3. Implement with **small, focused diffs**.
4. Run `pnpm build` (and any lint you set up) before finishing.
5. End with a **1-paragraph summary + next steps**.

### Branching to enable parallel sessions

* `main` stays stable.
* Each session works in its own branch (so you can run sessions in parallel):

  * `chore/scaffold`, `feat/layout`, `feat/home-stage`, `feat/editorial`, `feat/text`, `feat/work`, `feat/shop`, `chore/deploy`.

---

## 2) YES: create MD guideline files (so Codex can “re-load” context)

Create these **small** docs (each ideally < 150 lines):

* `docs/PROJECT_CONTEXT.md`
  **Non-negotiables**: English-only, Cloudflare Pages, static-first, monochrome, no-scroll rules, inquiry-only shop, etc.
* `docs/ROUTES.md`
  Route map + which pages are no-scroll vs scroll.
* `docs/UI_RULES.md`
  Tokens (B/W), hairlines, typography rules, motion rules, nav rules.
* `docs/CONTENT_MODEL.md`
  Folder structure for `src/content/*`, schemas, and how to add editorial chapters / text articles / PDFs.
* `docs/DEV_PLAYBOOK.md`
  Commands, branch naming, build steps, deployment notes.

These docs are your “context reset”. Every Codex session starts by reading them.

---

## 3) Dependency map (what must be sequential vs parallel)

### Must be sequential (blocking)

1. **Scaffold / tooling** (Astro + Tailwind + MDX + Cloudflare adapter + base structure)
2. **Base layouts + nav + no-scroll wrapper** (because every section uses them)

### Safe to run in parallel after base layouts exist (on separate branches)

* Home stage (rotating logo)
* Editorial system (content collections + routes + chapter rail)
* Text section (2 MDX articles + PDF download)
* Work section (galleries + logos PDF viewer)
* Shop section (inquiry-only, sold-out overlays)
* Deployment hardening (SEO/meta/404/sitemap) — can run late

---

## 4) Prompt templates (copy/paste)

### Template A — Planning prompt (use at start of each session)

```text
You are Codex working inside this repo.

First, read these files (do not read anything else unless needed):
- docs/PROJECT_CONTEXT.md
- docs/ROUTES.md
- docs/UI_RULES.md
- docs/CONTENT_MODEL.md (only if relevant)

Task:
<INSERT SESSION GOAL>

Constraints:
- Keep changes minimal and focused to this session’s scope.
- Prefer static generation; avoid backend/services.
- Avoid heavy dependencies; only add a library if truly needed.
- Respect no-scroll pages: Home + Editorial are 100vh with overflow hidden.

Output:
1) A step-by-step plan (max ~12 bullets)
2) Exact file list (create/modify)
3) Any risks/assumptions
Then stop. Do not implement yet.
```

### Template B — Execution prompt (run after the planning output)

```text
Proceed to implement the plan exactly.

Rules:
- Make small cohesive commits (or at least cohesive diffs).
- Do not refactor unrelated code.
- Ensure `pnpm build` succeeds.
- At the end, summarize what changed + how to test locally.
```

---

## 5) Concrete session prompts (planning + execution)

### Session 0 — Create canonical docs (HIGH leverage)

**Branch:** `chore/docs-foundation`
**Planning prompt**

```text
Read: Client-Brief-vision.txt (if present), and any existing AGENTS/README.
Goal: Create concise canonical docs to keep future Codex sessions within context.

Create:
- docs/PROJECT_CONTEXT.md
- docs/ROUTES.md
- docs/UI_RULES.md
- docs/CONTENT_MODEL.md
- docs/DEV_PLAYBOOK.md

Each doc must be short, clear, and reflect these decisions:
- English only, static-first
- Cloudflare Pages deploy from GitHub
- Home: rotating logo centerpiece
- Editorial: chapters list navigation, multiple separate pieces
- No scroll: Home + Editorial. Scroll: Shop/Text/Work
- Shop is inquiry/IG DM + fallback email (no checkout MVP)
- Logos hosted as PDFs on-site
- Text: 2 MDX articles + PDF download

Output plan + file list only.
```

**Execution prompt**

```text
Implement the docs as planned. Keep them short and “source of truth”.
```

---

### Session 1 — Scaffold Astro + Tailwind + MDX + Cloudflare Pages adapter

**Branch:** `chore/scaffold`
**Planning prompt**

```text
Read docs/PROJECT_CONTEXT.md and docs/DEV_PLAYBOOK.md (if exists).
Goal: Initialize an Astro + TS + Tailwind + MDX project ready for Cloudflare Pages.

Requirements:
- Astro + TypeScript
- Tailwind configured with CSS variables/tokens approach
- MDX support
- Cloudflare adapter (Pages)
- Basic folder skeleton:
  src/layouts, src/components, src/pages, src/content
  public/assets, public/pdfs
- Add minimal README with local dev + build commands

Output plan + file list only.
```

**Execution prompt**

```text
Implement scaffold. Ensure pnpm build works. Provide commands to run locally.
```

---

### Session 2 — Base layouts, hairline nav, and no-scroll wrapper

**Branch:** `feat/layout`
**Planning prompt**

```text
Read:
- docs/PROJECT_CONTEXT.md
- docs/ROUTES.md
- docs/UI_RULES.md

Goal:
Implement the global layout system:
- BaseLayout (shared nav/footer)
- NoScrollLayout (100vh, overflow hidden) for Home + Editorial
- Hairline top nav (small, unobtrusive)
- Global styles: monochrome tokens, hairline borders, typography scale (minimal)

Acceptance:
- / renders without scroll
- /editorial and /editorial/* can be built later with NoScrollLayout
- Nav is visible but visually minimal

Output plan + file list only.
```

**Execution prompt**

```text
Implement layouts + global styles. Keep components minimal. Ensure pnpm build passes.
```

---

### Session 3 — Home stage: rotating logo centerpiece (no GLB)

**Branch:** `feat/home-stage`
**Planning prompt**

```text
Read:
- docs/UI_RULES.md
- docs/ROUTES.md

Goal:
Build the Home page as a single viewport stage:
- Centerpiece: rotating logo (SVG preferred; fallback PNG)
- Use subtle motion, respect prefers-reduced-motion
- Nav remains unobtrusive (already built)
- Ensure NO vertical scroll

Implementation notes:
- If logos exist in repo, use them. Otherwise create placeholder.
- Use CSS transforms; keep JS minimal.

Output plan + file list only.
```

**Execution prompt**

```text
Implement home stage. Ensure no scroll. Add reduced-motion handling.
```

---

### Session 4 — Editorial system: pieces + chapters list navigation (no scroll)

**Branch:** `feat/editorial`
**Planning prompt**

```text
Read:
- docs/CONTENT_MODEL.md
- docs/ROUTES.md
- docs/UI_RULES.md

Goal:
Implement Editorial:
- /editorial index page: list of editorial pieces (cards)
- /editorial/[piece]/[chapter] pages generated from content
- No-scroll viewport layout:
  - Left rail: chapter list (click to navigate)
  - Main panel: chapter content
  - Occasional prev/next between chapters
- Keyboard support: up/down (optional) or left/right for prev/next (minimal)

Content approach:
- Use Astro content collections under src/content/editorial/<piece>/*.mdx
- Provide at least 1 demo piece with 2–3 chapters as example

Output plan + file list only.
```

**Execution prompt**

```text
Implement editorial system. Keep it static. Add sample content. Ensure pnpm build passes.
```

---

### Session 5 — Text section: two MDX articles + PDF download

**Branch:** `feat/text`
**Planning prompt**

```text
Read:
- docs/ROUTES.md
- docs/CONTENT_MODEL.md
- docs/UI_RULES.md

Goal:
Implement Text:
- /text index with 2 article cards + 1 PDF card
- /text/[slug] for the two MDX articles
- PDF download link from index + article pages
- Scroll enabled
- Reading mode typography (measure/line height) per UI rules

Output plan + file list only.
```

**Execution prompt**

```text
Implement Text section with two sample MDX articles and a placeholder PDF path. Ensure pnpm build passes.
```

---

### Session 6 — Work section: galleries + Logos as on-site PDFs

**Branch:** `feat/work`
**Planning prompt**

```text
Read:
- docs/ROUTES.md
- docs/UI_RULES.md
- docs/CONTENT_MODEL.md

Goal:
Implement Work:
- /work index: Tattoos / Jewellery / Logos
- Tattoos + Jewellery: image galleries (static)
- Logos:
  - /work/logos index: list/grid of logo PDF entries
  - /work/logos/[slug]: PDF viewer embed + download button
- Keep it static; no external links for logos.

Output plan + file list only.
```

**Execution prompt**

```text
Implement Work section with placeholder gallery items and a sample PDF entry. Ensure pnpm build passes.
```

---

### Session 7 — Shop section: inquiry-only, sold-out overlays, IG DM + email fallback

**Branch:** `feat/shop`
**Planning prompt**

```text
Read:
- docs/ROUTES.md
- docs/UI_RULES.md
- docs/PROJECT_CONTEXT.md

Goal:
Implement Shop:
- /shop index: collections grid (Love, Love Classic, Shams, Prints)
- /shop/[collection]: items grid
- Every item is SOLD OUT by design
- Primary CTA: “DM to inquire” (Instagram deep link with prefilled message)
- Secondary CTA: email fallback
- Scroll enabled
- No payments/checkout flows.

Output plan + file list only.
```

**Execution prompt**

```text
Implement Shop inquiry-only. Make CTAs consistent, minimal, and high contrast. Ensure pnpm build passes.
```

---

### Session 8 — Deployment hardening (Cloudflare Pages) + SEO basics

**Branch:** `chore/deploy`
**Planning prompt**

```text
Read:
- docs/DEV_PLAYBOOK.md
- docs/PROJECT_CONTEXT.md

Goal:
Finalize deployment readiness for Cloudflare Pages:
- Confirm adapter/build output is compatible with Pages
- Add basic SEO: titles, meta, OpenGraph defaults
- Add sitemap + robots.txt (static is fine)
- Add 404 page
- Document Cloudflare Pages settings: build command, output, node version assumptions

Output plan + file list only.
```

**Execution prompt**

```text
Implement deployment hardening + docs. Ensure pnpm build passes. Provide Cloudflare Pages configuration instructions.
```

---

## 6) One extra guideline to prevent Codex drift

At the top of **every** session prompt, add:

* “Do not introduce a CMS.”
* “Do not add payment integration.”
* “Do not add new design themes; strictly monochrome.”

This keeps scope from expanding.

---

## Final answer: should you create MD files for agents?

**Yes — absolutely.** In your scenario (parallel sessions + small context windows + no maintenance), the docs are your “persistent memory.” Keep them short, opinionated, and treated as the source of truth. Then every Codex session begins by reading only those docs and stays aligned.

If you want, paste your repo root listing (or current structure) and I’ll tailor the exact file paths and the minimal doc contents so Session 0 can be run immediately without ambiguity.

